services:
  - type: web
    name: brisbane2032-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python db/init_db.py
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
